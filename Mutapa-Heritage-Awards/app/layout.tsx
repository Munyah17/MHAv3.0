import type { Metadata, Viewport } from 'next'
import { Playfair_Display, Inter } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const playfair = Playfair_Display({ 
  subsets: ["latin"],
  variable: '--font-playfair',
  display: 'swap',
})

const inter = Inter({ 
  subsets: ["latin"],
  variable: '--font-inter',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Munhumutapa Heritage Awards | Our Heritage, Our Future',
  description: 'The Munhumutapa Heritage Awards is a premier cultural institution dedicated to the preservation, celebration, and promotion of Zimbabwe\'s rich historical legacy. Building Zimbabwe brick by brick - United We Stand.',
  keywords: ['Zimbabwe', 'Heritage', 'Awards', 'Culture', 'Munhumutapa', 'African Heritage', 'Cultural Preservation', 'Zimbabwe Open for Business', 'United We Stand'],
  generator: 'v0.app',
  openGraph: {
    title: 'Munhumutapa Heritage Awards',
    description: 'Celebrating the guardians who preserve Zimbabwe\'s rich cultural legacy for future generations.',
    type: 'website',
  },
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export const viewport: Viewport = {
  themeColor: '#0a0908',
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="bg-background">
      <body className={`${playfair.variable} ${inter.variable} font-sans antialiased bg-background`}>
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
